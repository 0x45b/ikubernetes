kind: Ingress
apiVersion: extensions/v1beta1
metadata:
  name: grafana
  namespace: monitoring
spec:
  rules:
  - host: prometheus-grafana.fdisk.cc
    http:
      paths:
      - path: /
        backend:
          serviceName: grafana
          servicePort: 3000
